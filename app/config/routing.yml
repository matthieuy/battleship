# AppBundle
app:
    resource: "@AppBundle/Controller/"
    type:     annotation

# UserBundle
user:
    resource: "@UserBundle/Controller/"
    type:     annotation

# FosUserBundle
fos:
    resource: "bundles/fos_user_routing.yml"

# MatchBundle
match:
    resource: "@MatchBundle/Controller/"
    type:     annotation

# BonusBundle
bonus:
    resource: "@BonusBundle/Controller/"
    type:     annotation

# ChatBundle
chat:
    resource: "@ChatBundle/Controller/"
    type:     annotation

# NotificationBundle
notification:
    resource: "@NotificationBundle/Controller/"
    type:     annotation

# StatsBundle
stats:
    resource: "@StatsBundle/Controller/"
    type:     annotation

# JS Translation
_bazinga_jstranslation:
    resource: "@BazingaJsTranslationBundle/Resources/config/routing/routing.yml"
